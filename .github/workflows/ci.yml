name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Test Swift package
        run: swift test
      - name: Verify pinned pi-ai upstream
        run: ./Scripts/check-upstream.sh
